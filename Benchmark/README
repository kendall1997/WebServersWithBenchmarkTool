To compile

$ make

Manual Execution

$ ./Benchmark autogestion.metrotel.com.ar 80 speedtest/archivo10MB.zip 2 2

This will run the benchmark with the host, port and uri given, with 2 threads and with 2 cycles.

The output will be in the terminal's standard output, to store the result in a file in disk, run:

$ ./Benchmark autogestion.metrotel.com.ar 80 speedtest/archivo10MB.zip 2 2 > data.csv